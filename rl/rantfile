
rll_objs = %w(rlcore.o)

file "librl.a" => rll_objs do |t|
	sys "ar rsv librl.a #{rll_objs.join(' ')}"
	sys.cp "librl.a",".."
end

desc "Cleanup..."
task :clean do
	x = Dir["*.{bak,o,obj,a,lib}"]

	if x.length > 0
		sys.rm_rf x
	end
end
